{
  "name": "foundry-quest-board",
  "version": "1.1.0",
  "description": "Maiyalis: Quest Board — immersive, physical-looking notice boards of parchment notices, wanted posters, rumours and jobs for FoundryVTT.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "catalog": "node tools/build-catalog.mjs",
    "build": "npm run catalog && tsc --noEmit && vite build",
    "watch": "npm run catalog && vite build --watch",
    "dev": "npm run catalog && vite build --watch",
    "typecheck": "npm run catalog && tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
